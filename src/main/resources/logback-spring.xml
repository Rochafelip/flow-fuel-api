<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Padroes de log por perfil (ADR-008).
         - dev/test: console colorido, legivel para humanos.
         - prod: JSON estruturado via logstash-logback-encoder, com requestId/userId do MDC.
         O Sentry intercepta logs >= ERROR automaticamente (sentry-logback). -->

    <!-- Lido antes do appender inicializar; default vazio desliga o SDK sem WARN. -->
    <springProperty name="sentryDsn" source="sentry.dsn" defaultValue=""/>

    <springProfile name="dev,test,default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%X{requestId:-}] [%X{userId:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <customFields>{"app":"flowfuel"}</customFields>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
            </encoder>
        </appender>

        <!-- Sentry: somente eventos ERROR sao reportados; WARN/INFO ficam so no log. -->
        <appender name="SENTRY" class="io.sentry.logback.SentryAppender">
            <options>
                <dsn>${sentryDsn}</dsn>
            </options>
            <minimumEventLevel>ERROR</minimumEventLevel>
            <minimumBreadcrumbLevel>INFO</minimumBreadcrumbLevel>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON"/>
            <appender-ref ref="SENTRY"/>
        </root>
    </springProfile>

</configuration>
